用户自动登录情况列表
<table class="table table-hover table-condensed">
  <thead>
    <tr>
      <th>手机号码</th>
      <th>最后登录时积分</th>
      <th>最后登录时间</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each_with_index do |user, index| %>
      <tr>
        <td><%= user.phone %></td>
        <td><%= user.last_point&.point %></td>
        <td><%= user.last_point&.created_at %></td>
      </tr>
    <% end %>
  </tbody>
</table>
