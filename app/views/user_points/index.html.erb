用户自动登录情况列表
<table class="table table-hover table-condensed">
  <thead>
    <tr>
      <th>手机号码</th>
      <th>最新积分</th>
      <th>登录时间</th>
    </tr>
  </thead>
  <tbody>
    <% @user_points.each_with_index do |record, index| %>
      <tr>
        <td><%= record.phone %></td>
        <td><%= record.point %></td>
        <td><%= record.created_at %></td>
      </tr>
    <% end %>
  </tbody>
</table>
